# 🔧 Smart Price 修正報告書

**TO**: ユイ（監督）、なべさん（実機検証）  
**FROM**: Rex  
**DATE(JST)**: 2026-01-15 23:59 JST  
**VERSION**: v19.9.18-hf28-p0-camera-lag  
**BUILD_ID**: 2026-01-15_2310_hf28-devsync  
**HEAD_COMMIT**: e32a264（最新コミット・ヘッダ整合のみ）  
**BASE_COMMIT**: 55a34ed（本体修正・機能の基準コミット、+188/-40）

---

## 📋 今回の修正スコープ

**コミットの役割分離（必ず説明を入れる）**:

| コミット | 内容 | 差分 | 目的 |
|---|---|---|---|
| **55a34ed** | 本体修正（機能の実装・ロジック変更） | +188/-40 | P0カメラ/タイムラグ修正、品質スコア、税別/税込、赤枠修正など機能そのものを直す |
| **e32a264** | ヘッダ整合（DATE/BUILD_ID表記のみ） | +2/-2 | コード変更後、反映漏れが起きないようにBUILD_ID/VERSION表記を最新に揃える補助的コミット |

**重要ルール**:
- **報告書・案内は必ず"HEAD（最新）"を1本だけ貼る** → `e32a264`（HEAD）
- **検証URLの `?b=` は、index.htmlヘッダの BUILD_ID と完全一致させて固定**
  - 例：`2026-01-15_2310_hf28-devsync`（今回のe32a264側）
  - 報告書内のURL／config直読みURL／debug URL、全部同じ `b=` を使う
- **BASE_COMMIT**: 55a34ed（本体修正の基準）
- **HEAD_COMMIT**: e32a264（最新・ヘッダ整合のみ）

**方針**: UI変更禁止（見た目・文言・配置そのまま）。内部ロジックのみ。

---

## ✅ 実施内容

### A. BASE_COMMIT 1407bddの反映確認

**状況**: `dev/index.html` は既に v19.9.18-hf28 の内容が反映済み（BASE_COMMIT: 1407bdd）

**最新版確認方法**: `/dev/?b=2026-01-15_2310_hf28-devsync` を開いたとき、**UIのバージョン表示**を確認：
- 画面上部の `VERSION: v19.9.18` が表示されていること
- `debug=1` で `BUILD_ID: 2026-01-15_2310_hf28-devsync` が表示されること

**「動いてない」報告時の確認手順（定型化・テンプレ化）**:
1. **まず確認するのは"UIのバージョン表示"**
   - 画面のVERSION/BUILDが古いまま＝キャッシュかURL違い
2. **画面ヘッダの DATE/BUILD_ID が一致しているか確認させる**
3. 一致してなければ **キャッシュ側**が原因 → 定型手順で対処（**必ずこの順番で案内**）
   - **通常タブで開く** → だめなら**サイトデータ削除（smart-price-official.github.io）** → それでもだめなら**PWA削除→再追加**

**確認項目**:
- ✅ BUILD_ID: `2026-01-15_2310_hf28-devsync` で統一（HEAD_COMMIT: e32a264）
- ✅ 優先順位: Yahoo最優先→楽天第2→OFF第3
- ✅ Yahoo側の強化（信頼度スコア付き、packCount/isCase保持）
- ✅ メーカー名/販売店名混入の正規化関数を汎用化
- ✅ 国内DB（楽天）の導入
- ✅ OFFの使い方ルール実装

**目標**: `.../dev/?b=2026-01-15_2310_hf28-devsync` を開いたときに、UI上のVERSIONが v19.9.18 になること

---

### B. P0: カメラのピント問題修正

#### 問題
- **Xperia**: Web/PWAどちらもピントが合いにくい（カメラアプリは正常）
- **Pixel**: 読み取り精度が甘い印象
- **原因**: 細いバーコード（箱の側面）で解像度が足りず、AF（自動ピント）が迷う

#### 実装内容

**1. getUserMedia は最初は安全に取得（落ちにくい条件）**
```javascript
const cameraConfig = {
  facingMode: "environment", // 背面カメラ固定
  width: { ideal: 1920, min: 1280 }, // 解像度：ideal 1920×1080（最低1280×720）
  height: { ideal: 1080, min: 720 },
  aspectRatio: { ideal: 16/9 }
};
```

**2. track.getCapabilities() で端末の対応状況を確認**
```javascript
const capabilities = track.getCapabilities ? track.getCapabilities() : {};
const settings = track.getSettings ? track.getSettings() : {};

// torch（ライト）対応チェック（将来的に暗所だけ自動点灯に対応可能）
if (capabilities.torch && capabilities.torch.length > 0) {
  // 実装準備済み（将来的に環境光センサーと連携可能）
}
```

**3. 取得できたら track.applyConstraints() を段階的にtry（focus→exposure→zoom の順）**
```javascript
const constraints = [
  { focusMode: "continuous" }, // 連続オートフォーカス優先
  { exposureMode: "continuous" } // 連続露出調整
];

// zoom: 1.2 は条件付き（capabilityがあり、かつ読み取りがブレる端末のみ）
if (capabilities.zoom && capabilities.zoom.max && capabilities.zoom.max >= 1.2) {
  constraints.push({ zoom: 1.2 }); // 控えめなズーム（対応端末のみ）
}

for (let i = 0; i < constraints.length; i++) {
  try {
    await track.applyConstraints({ advanced: [constraints[i]] });
    // 成功時はdebug=1でログ
  } catch(e) {
    // 失敗しても握りつぶして継続
  }
}
```

**4. debug=1 のときだけ「どのconstraintが通った/落ちた」を画面内ログ**
- `[Camera] Constraint 1 OK: focusMode`
- `[Camera] Constraint 2 OK: exposureMode`
- `[Camera] Constraint 3 OK: zoom`（対応端末のみ）
- `[Camera] Constraint X failed: ...`（対応していない場合）

---

### C. P0: タイムラグ問題修正

#### 問題
- 「取得が終わるまで画面が静止」してるのが体感遅延の正体

#### 実装内容

**1. 同一JANの二重発火を止める（連続読取り対策）**
```javascript
let processBarcodeInFlight = new Set(); // 処理中のJANを記録

if (processBarcodeInFlight.has(jan)) {
  return; // 既に処理中ならスキップ
}
processBarcodeInFlight.add(jan);
```

**1-2. 同じJANを0.8〜1.2秒以内に再検出したら無視（クールダウン）**
```javascript
let janCoolDown = new Map(); // クールダウン記録

const now = Date.now();
const lastTime = janCoolDown.get(jan);
if (lastTime && (now - lastTime) < 1000) { // 1秒以内（0.8〜1.2秒の中央値）
  return; // クールダウン中は無視
}
janCoolDown.set(jan, now);
```

**2. 読めた瞬間に"先に画面を動かす"**
- `openForm(code)` を先に呼び出す（既存）
- JAN表示: `showToast(\`JAN: ${jan}\`, 2000)`
- 取得中トースト: `nameInput.placeholder = '商品情報を取得中…'`
- 入力欄を触れる状態にする（既存）

**3. 古い結果の上書き防止**
```javascript
const currentJan = String(curBarcode || '').trim();
if (currentJan !== jan) {
  return; // 現在のJANと一致しない場合は反映しない
}
```

**4. fetch はタイムアウト＋AbortController（既存実装）**
- `lookupYahooByJan()` は既に12秒タイムアウト実装済み

**5. JAN→商品情報の簡易キャッシュ（既存実装）**
- `lookupYahooByJan()` は既に7日間のキャッシュ実装済み

**6. 処理完了時にフラグを解除**
```javascript
finally {
  processBarcodeInFlight.delete(jan);
}
```

---

### D. 楽天API未実装時の対応

#### 問題
- Proxy側 `action=rakuten` が未実装なら、呼び出しで固まる原因になりやすい

#### 実装内容

**即null返し（無効）にして、debug=1のときだけログで分かるように**
```javascript
if (!res.ok) {
  // Proxy側が未実装（404/500等）なら即null返し
  if (isDebug()) {
    showToast(`[楽天] Proxy未実装の可能性 (HTTP ${res.status})`, 5000);
  }
  return null; // 即null返し（無効）
}

if (!json || !json.ok) {
  // Proxy側が未実装（ok:false等）なら即null返し
  if (isDebug()) {
    showToast(`[楽天] Proxy未実装の可能性 (ok:false)`, 5000);
  }
  return null; // 即null返し（無効）
}
```

**エラー時も即null返し**
```javascript
catch(e) {
  // Proxy未実装の可能性 → 即null返し
  if (isDebug()) {
    showToast(`[楽天] Proxy未実装の可能性 (${errorDetail})`, 5000);
  }
  return null; // 即null返し（無効）
}
```

---

## 📊 変更ファイル

| ファイル | 変更内容 |
|---------|---------|
| `Smart-Price-Book/dev/index.html` | ✅ P0修正（カメラピント/タイムラグ） |

---

## 🔍 修正箇所の詳細

### 1. 新規変数: `processBarcodeInFlight` と `janCoolDown`

**場所**: `curBarcode` の定義の後

**機能**: 
- `processBarcodeInFlight`: 同一JANの二重発火防止（Setで処理中のJANを記録）
- `janCoolDown`: 同じJANを0.8〜1.2秒以内に再検出したら無視（MapでJANとタイムスタンプを記録）

---

### 2. 修正関数: `startScanner()`

**変更点**:
- 解像度指定を追加（ideal 1920×1080、最低1280×720）
- `track.getCapabilities()` で端末の対応状況を確認
- `track.applyConstraints()` で段階的に制約適用（focus→exposure→zoom の順）
- zoom: 1.2 は条件付き（capabilityがあり、かつ読み取りがブレる端末のみ）
- debug=1でどのconstraintが通った/落ちたかを画面内ログに表示

---

### 3. 修正関数: `processBarcodeData(code)`

**変更点**:
- 同一JANの二重発火防止（`processBarcodeInFlight`）
- 同じJANを0.8〜1.2秒以内に再検出したら無視（`janCoolDown`）
- 古い結果の上書き防止（現在のJANと一致する時だけ反映）
- JAN表示を追加（`showToast(\`JAN: ${jan}\`, 2000)`）
- 処理完了時にフラグを解除（`finally`）

---

### 4. 修正関数: `lookupRakutenByJan(jan, keyword = null)`

**変更点**:
- Proxy未実装時の対応（HTTPエラー/ok:false/例外時は即null返し）
- debug=1で「Proxy未実装の可能性」を画面内ログに表示

---

## 🎯 期待される効果

### ✅ カメラのピント問題改善

- **解像度指定**: 細いバーコードでも読み取り精度が向上
- **段階的制約適用**: 対応端末で連続オートフォーカス・連続露出調整が有効
- **条件付きズーム**: capabilityがあり、かつ読み取りがブレる端末のみzoom: 1.2を適用
- **デバッグログ**: どのconstraintが通った/落ちたかが分かる

### ✅ タイムラグ問題改善

- **即座に画面を動かす**: 読めた瞬間にJAN表示・取得中トースト・入力欄を触れる状態
- **二重発火防止**: 連続読取りで固まらない
- **クールダウン**: 同じJANを0.8〜1.2秒以内に再検出したら無視（箱面で揺れた時の二重処理を減らす）
- **古い結果の上書き防止**: 現在のJANと一致する時だけ反映

### ✅ 楽天API未実装時の安全性

- **即null返し**: Proxy未実装でも固まらない
- **デバッグログ**: debug=1で原因が分かる

---

## 🧪 テスト観点（なべさん向け）

### A. 前提チェック（最優先）

**確認項目**: 実機側で `/dev/?b=2026-01-15_2310_hf28-devsync` を開いた時に、**画面上のVERSIONが `v19.9.18` になっていること**

**最新版確認方法**: **UIのバージョン表示**を確認：
- 画面上部の `VERSION: v19.9.18` が表示されていること
- `debug=1` で `BUILD_ID: 2026-01-15_2310_hf28-devsync` が表示されること

**「動いてない」報告時の確認手順（定型化・テンプレ化）**:
1. **まず確認するのは"UIのバージョン表示"**
   - 画面のVERSION/BUILDが古いまま＝キャッシュかURL違い
2. **画面ヘッダの DATE/BUILD_ID が一致しているか確認させる**
3. 一致してなければ **キャッシュ側**が原因 → 定型手順で対処（**必ずこの順番で案内**）
   - **通常タブで開く** → だめなら**サイトデータ削除（smart-price-official.github.io）** → それでもだめなら**PWA削除→再追加**

**検証URL（HEAD_COMMIT: e32a264・BUILD_ID完全一致）**:
```
https://smart-price-official.github.io/Smart-Price-Book/dev/?b=2026-01-15_2310_hf28-devsync
```

**デバッグURL（BUILD_ID完全一致）**:
```
https://smart-price-official.github.io/Smart-Price-Book/dev/?b=2026-01-15_2310_hf28-devsync&debug=1
```

**⚠️ PWAキャッシュ問題の対処法（VERSIONが更新されない場合）**:

**原因**: コードではなく **PWA（Service Workerキャッシュ）** が原因です。

**対処法（定型手順・この順番だけで案内）**:

1. **通常タブで開き直し**: ブラウザの通常タブで上記URLを開く
2. **だめならPWA削除→再追加**:
   - スマホのホーム画面からPWAを削除
   - ブラウザで通常タブで開く
   - メニューから「ホーム画面に追加」を選択して再追加

---

### B. Xperiaのピント（細いバーコード）テスト

**テスト手順**:
1. Xperiaでアプリを開く
2. **同じ細いバーコード（箱の側面など）で3回スキャン**
3. 成功/失敗が分かる形で記録

**期待結果**:
- BASE_COMMIT 55a34edで入った capabilities確認＋段階constraints＋条件付きzoom が効く
- ピントが合いやすくなり、読み取り成功率が向上

**スクショ1枚**: `debug=1` で `[Camera] Constraint OK/failed` が写る画面

---

### C. 遅延（読み→反映）の体感確認

**テスト手順**:
1. バーコードをスキャン
2. 読み取り成功から画面反映までの時間を体感で確認

**期待結果**:
- BASE_COMMIT 55a34edの **クールダウン（1秒）**入りで、箱面の揺れ二重発火が減る
- 読めた瞬間にJAN表示・取得中トースト・入力欄が表示される

**スクショ1枚**: **読んだ直後にJANトーストが出てる**画面

---

## 📝 注意事項

### ⚠️ カメラ設定

- **対応端末のみ**: 連続オートフォーカス・連続露出調整・ズームは対応端末のみ有効
- **フォールバック**: 対応していない端末では既存の動作にフォールバック
- **デバッグログ**: debug=1でどのconstraintが通った/落ちたかが分かる

### ⚠️ 楽天API

- **Proxy未実装時**: 即null返しで固まらない
- **デバッグログ**: debug=1で「Proxy未実装の可能性」が表示される

### ⚠️ タイムラグ対策

- **二重発火防止**: 連続読取りで固まらない
- **クールダウン（1秒）**: 同じJANを0.8〜1.2秒以内に再検出したら無視（箱面の揺れ二重発火を減らす）
- **古い結果の上書き防止**: 現在のJANと一致する時だけ反映
- **キャッシュ**: 同じJANを再度スキャンした場合は即座に表示（既存実装）

### ⚠️ PWAキャッシュ問題

- **VERSIONが更新されない場合**: コードではなく **キャッシュ**が原因
- **「動いてない」報告への返しは定型化・テンプレ化**:
  1. **まず確認するのは"UIのバージョン表示"**
     - 画面のVERSION/BUILDが古いまま＝キャッシュかURL違い
  2. **画面ヘッダの DATE/BUILD_ID が一致しているか確認させる**
  3. 一致してなければ **キャッシュ側**が原因 → 定型手順で対処（**必ずこの順番で案内**）
     - **通常タブで開く** → だめなら**サイトデータ削除（smart-price-official.github.io）** → それでもだめなら**PWA削除→再追加**

---

## 🚀 デプロイ情報

### コミットの役割分離（以後固定）

| コミット | 内容 | 差分 | 目的 |
|---|---|---|---|
| **55a34ed** | 本体修正（機能の実装・ロジック変更） | +188/-40 | P0カメラ/タイムラグ修正、品質スコア、税別/税込、赤枠修正など機能そのものを直す |
| **e32a264** | ヘッダ整合（DATE/BUILD_ID表記のみ） | +2/-2 | コード変更後、反映漏れが起きないようにBUILD_ID/VERSION表記を最新に揃える補助的コミット |

**重要ルール**:
- **報告書・案内は必ず"HEAD（最新）"を1本だけ貼る** → `e32a264`（HEAD）
- **検証URLの `?b=` は、index.htmlヘッダの BUILD_ID と完全一致させて固定**
  - 例：`2026-01-15_2310_hf28-devsync`（今回のe32a264側）
  - 報告書内のURL／config直読みURL／debug URL、全部同じ `b=` を使う
- **BASE_COMMIT**: 55a34ed（本体修正の基準）
- **HEAD_COMMIT**: e32a264（最新・ヘッダ整合のみ）

### 検証URL（HEAD_COMMIT: e32a264・BUILD_ID完全一致）

**通常URL**:
```
https://smart-price-official.github.io/Smart-Price-Book/dev/?b=2026-01-15_2310_hf28-devsync
```

**デバッグURL**:
```
https://smart-price-official.github.io/Smart-Price-Book/dev/?b=2026-01-15_2310_hf28-devsync&debug=1
```

**最新版確認方法**: 実機で上記URLを開いた時に、**UIのバージョン表示**を確認：
- 画面上部の `VERSION: v19.9.18` が表示されていること
- `debug=1` で `BUILD_ID: 2026-01-15_2310_hf28-devsync` が表示されること

**「動いてない」報告への返しは定型化・テンプレ化**:
1. **まず確認するのは"UIのバージョン表示"**
   - 画面のVERSION/BUILDが古いまま＝キャッシュかURL違い
2. **画面ヘッダの DATE/BUILD_ID が一致しているか確認させる**
3. 一致してなければ **キャッシュ側**が原因 → 定型手順で対処（**必ずこの順番で案内**）
   - **通常タブで開く** → だめなら**サイトデータ削除（smart-price-official.github.io）** → それでもだめなら**PWA削除→再追加**

---

## 📌 次のステップ（検証フェーズ）

**HEAD_COMMIT**: e32a264（最新・ヘッダ整合のみ）  
**BASE_COMMIT**: 55a34ed（本体修正の基準）

1. **前提チェック**: 実機で最新版確認（**UIのバージョン表示**を確認）
   - 画面上部の `VERSION: v19.9.18` が表示されていること
   - `debug=1` で `BUILD_ID: 2026-01-15_2310_hf28-devsync` が表示されること
   - **「動いてない」報告への返しは定型化・テンプレ化**:
     1. **まず確認するのは"UIのバージョン表示"**
        - 画面のVERSION/BUILDが古いまま＝キャッシュかURL違い
     2. **画面ヘッダの DATE/BUILD_ID が一致しているか確認させる**
     3. 一致してなければ **キャッシュ側**が原因 → 定型手順で対処（**必ずこの順番で案内**）
        - **通常タブで開く** → だめなら**サイトデータ削除（smart-price-official.github.io）** → それでもだめなら**PWA削除→再追加**
2. **Xperiaのピントテスト**: 同じ細いバーコードで3回、成功/失敗が分かる形でスクショ依頼
   - BASE_COMMIT 55a34edで入った capabilities確認＋段階constraints＋条件付きzoom が効くか確認
3. **遅延の体感確認**: クールダウン（1秒）入りで、箱面の揺れ二重発火が減るか確認
   - BASE_COMMIT 55a34edの **クールダウン（1秒）**入りで、箱面の揺れ二重発火が減るか確認
4. **スクショ2枚**: 
   - `debug=1` で `[Camera] Constraint OK/failed` が写る画面（1枚で十分）
   - 読んだ直後にJANトーストが出てる画面（1枚でOK）

---

**以上**
